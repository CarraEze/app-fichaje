import { StyleSheet } from "react-native"

export const globalStyles = StyleSheet.create({
    view: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: "#000",
    }
});
